# TODOs

## [High] 修复 `equip` 脚本错误

- 现象：运行 GUT 集成测试时持续出现错误：
  - `Invalid call. Nonexistent function 'equip' in base 'Node2D (RifleWeapon)'`
- 主要定位：
  - `res://src/characters/player.gd:326` (`Player._equip_weapon`)
  - 调用链：`_equip_weapon -> _initialize_weapons -> _ready`
- 影响：
  - 多个 integration 测试执行时刷出脚本错误，影响日志质量与武器初始化可靠性判断。
- 建议修复方向：
  1. 核对 `RifleWeapon` 实际脚本接口（是否存在 `equip`，或已改名）。
  2. 在 `player.gd` 统一适配当前武器组件 API（必要时做兼容分支）。
  3. 为武器初始化补充最小回归测试，确保 `equip` 路径可执行。
